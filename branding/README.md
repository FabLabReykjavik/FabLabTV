# Branding

FabLabTV ships with a generic default logo:

```text
branding/fablab.png
```

Labs can upload their own logo from the Remote screen. Uploaded logos are saved as:

```text
branding/logo.png
```

The TV screen uses `logo.png` when it exists, otherwise it falls back to `fablab.png`.
